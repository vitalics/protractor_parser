{
  "name": "protractor_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx protractor conf.js",
    "driver-update": "npx webdriver-manager update",
    "driver-start": "npx webdriver-manager start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "protractor": "^5.4.2",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5",
    "webdriver-manager": "^12.1.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.12"
  }
}
